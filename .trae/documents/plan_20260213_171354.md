# 💎 精英风“灵动”界面重构计划

我已完成对您当前项目的界面分析，发现当前存在样式文件缺失（`advanced-style.css` 引用但不存在）和风格定位偏差（当前偏向赛博科技风，而非精英商务风）的问题。

我将调用 **UI设计智能体 (UI Designer Agent)** 为您执行以下重构：

## 1. 🎨 视觉风格升级：精英商务 (Elite Business)
将原本的暗黑科技风改造为高端、沉稳的**精英商务风**。
*   **配色方案**：
    *   **主色调**：深海蓝 (Deep Navy) - 象征专业与稳重。
    *   **辅助色**：香槟金 (Champagne Gold) - 点缀高贵质感。
    *   **背景**：高级灰/纯白 (Premium Grey/White) - 提升阅读舒适度。
*   **排版**：优化字体层级，使用更现代的无衬线字体，增加留白，提升呼吸感。

## 2. ✨ “灵动感”交互设计
*   **微交互**：按钮悬停、卡片点击增加平滑的过渡动画。
*   **入场动画**：页面加载时，元素按层级淡入上浮，消除生硬感。
*   **玻璃拟态**：在导航栏和卡片背景使用轻微的毛玻璃效果 (Glassmorphism)，增加界面的通透感和层次感。

## 3. 🛠️ 核心修复与功能保障
*   **修复 CSS 链接**：统一 CSS 文件引用，确保 `dashboard.ejs` 指向正确的样式文件，解决页面“裸奔”或样式丢失问题。
*   **防崩溃机制**：
    *   为所有图片添加加载失败的占位符。
    *   确保所有 EJS 模板变量都有默认值，防止因数据缺失导致的页面报错。

## 4. 📂 执行步骤
1.  **备份**：备份现有的 `views` 和 `public/css` 目录。
2.  **样式重写**：创建全新的 `public/css/elite-style.css`。
3.  **模板更新**：更新 `views/*.ejs` 文件，适配新样式类名，并修复 broken links。
4.  **智能体执行**：我将调用 **UI Designer** 智能体来生成高质量的代码。

> **是否确认执行此计划？** 确认后我将立即开始重构工作。