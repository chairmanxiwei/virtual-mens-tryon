已成功将AI搭配页面的标题文字颜色修改为白色。

### 修改内容
- **文件**：`nodejs-login-app/public/css/renaissance-style.css`
- **修改位置**：`.hero-title` 样式定义
- **添加属性**：`color: var(--text-color);`
- **颜色值**：`var(--text-color)` 对应 `#f0f0f0`（白色）

### 技术说明
- 修改了hero-title样式，添加颜色属性确保标题文字显示为白色
- 检查了reveal-text样式，确认它不会覆盖颜色设置
- 使用CSS变量保持样式一致性，便于后续维护

### 预期效果
- AI搭配页面的标题「AI 搭配师」现在会显示为白色
- 保持原有的字体大小、间距和动画效果
- 与页面整体深色背景形成良好对比，提升可读性