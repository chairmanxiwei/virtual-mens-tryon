I will unify the design of all pages (Login, Register, Wardrobe, AI Outfit, Virtual Try-on) to match the "Artistic Renaissance" style and ensure full Chinese localization.

### 1. Enhance Design System (`renaissance-style.css`)
I need to expand the CSS to support functional elements (forms, buttons, grids) that were present in the old style but adapted to the new "Dark & Gold" aesthetic.
-   **Add Button Styles**: Elegant gold-outlined buttons with hover effects.
-   **Add Form Styles**: Minimalist input fields with gold focus states.
-   **Add Component Styles**: Wardrobe grids, upload modals, and card layouts using the glassmorphism/artistic style.

### 2. Update Login & Register Pages
-   **Structure**: Convert to a full-screen "Hero" layout.
-   **Visuals**: Use the "Art Card" container for the login form.
-   **Localization**: Translate "Elite Business Edition" and all labels to high-quality Chinese (e.g., "绅士衣橱", "尊享账号").
-   **Animation**: Add GSAP entry animations and the custom cursor.

### 3. Update Feature Pages (Wardrobe, AI Outfit, Try-on)
-   **Navigation**: Replace the sidebar/topbar with the new floating `nav-overlay`.
-   **Layout**: Wrap content in `<section>` containers with smooth scrolling (Lenis).
-   **Components**:
    -   **Wardrobe**: Style the clothing grid as an art gallery.
    -   **AI Outfit**: Style the generation form to look like a "Request" card.
    -   **Virtual Try-on**: Style the 3D viewer and upload zone to be immersive.
-   **Localization**: Ensure all buttons, placeholders, and status messages are in Chinese.

### 4. Implementation Steps
1.  **Modify `public/css/renaissance-style.css`**: Add missing functional styles.
2.  **Rewrite `views/login.ejs` & `views/register.ejs`**.
3.  **Rewrite `views/wardrobe.ejs`**.
4.  **Rewrite `views/ai-outfit.ejs`**.
5.  **Rewrite `views/virtual-tryon.ejs`**.
