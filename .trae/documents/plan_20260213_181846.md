I will upgrade the three core modules of your Virtual Menswear project using free, open-source technologies.

### 1. Upgrade Wardrobe (Intelligent Recognition)
**Goal**: Replace hardcoded logic with real AI recognition.
-   **Solution**: Create a Python microservice (`services/image_analysis.py`) using **CLIP (OpenAI/HuggingFace)** for zero-shot classification and **Rembg** for background removal.
-   **Features**:
    -   **Precise Color**: Remove background first, then analyze the clothing area to distinguish "White Shirt" from "White Wall".
    -   **Smart Tagging**: Automatically detect Type (Suit/Casual), Style (Business/Street), and Material via CLIP.

### 2. Upgrade AI Outfit (Personalized & Metaphysics)
**Goal**: Add "Scenario + Metaphysics + Weather" context.
-   **Solution**: Create a new `AIRecommendationService`.
-   **Features**:
    -   **Weather**: Integrate **Open-Meteo API** (Free, no key) to fetch real-time temperature/weather.
    -   **Metaphysics (Wu Xing)**: Algorithm to calculate "Lucky Color" and "Element" based on current date/time.
    -   **LLM Integration**: Add a connector for Large Language Models (compatible with OpenAI/DeepSeek formats). I will implement a "Smart Template" fallback so it works immediately without an API key, but allows you to plug one in for "True AI" generation.

### 3. Upgrade Virtual Try-on (3D Modeling)
**Goal**: Fix the crash and enable interactive models.
-   **Solution**: Refactor `pytorch3d_modeling.py` and the frontend loader.
-   **Features**:
    -   **Fix 3D Generation**: Instead of a broken "Cylinder Man", I will implement a **Textured Mesh Generator**. It will map your uploaded photo onto a standardized 3D bust/mannequin mesh.
    -   **Interactive Viewer**: Update the Three.js frontend to properly load and rotate this textured model.
    -   **Fix Loading**: Resolve the path issues causing the "infinite loading" or crash.

### Implementation Steps
1.  **Environment**: Create `requirements.txt` with necessary AI libraries (`transformers`, `rembg`, `torch`).
2.  **Backend (Python)**: Implement `image_analysis.py` and upgrade `pytorch3d_modeling.py`.
3.  **Backend (Node.js)**: Update services to call these Python scripts and handle the data.
4.  **Frontend**: Update `ai-outfit.ejs` to show Weather/Lucky info and `virtual-tryon.ejs` to render the new 3D model format.
5.  **Testing**: Verify all three modules work together.
